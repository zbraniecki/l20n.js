(function(global) {

  /**
   * allow for narrowing down per querySelector especially for attributes
   * Example: 'only when elements with l10n-id and l10n-args are altered'.
   **/
  global.mozMutationObserver = {
  };

})(this);

